{
  "name": "apex-hq-functions",
  "private": true,
  "type": "module",
  "engines": { "node": "22" },
  "main": "booking-only.js",
  "scripts": {
    "lint": "node --check booking-only.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "latest",
    "firebase-functions": "latest",
    "luxon": "latest"
  }
}
